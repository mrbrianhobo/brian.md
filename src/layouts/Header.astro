---
import ThemeToggle from "../components/ThemeToggle";
---

<div class="flex w-full flex-row justify-between text-muted">
  <div class="m-4 self-center truncate">
    <div class="bg-muted/10 rounded px-1">
      <h1 class="text-lg font-code font-normal truncate">~{Astro.url.pathname === "/" ? "/BRIAN.MD" : Astro.url.pathname}</h1>
    </div>
  </div>
  <ThemeToggle client:only="react" />
</div>
